# Examples

This directory contains concrete `foga` configurations derived from real
projects.

- [`qupled`](qupled/README.md): native CMake build, Python packaging, pytest
  suites, and a `ctest` native test runner with MPI-related profiles.
- [`pybind11`](pybind11/README.md): pinned containerized example that provisions
  the upstream project in Docker and runs `foga` against it.
