project:
  name: python-only-example

build:
  python:
    backend: python-build
    launcher: ["uv", "run"]

install:
  targets:
    dev:
      backend: uv
      extras: ["dev"]

clean:
  paths:
    - dist
    - .venv
